import { KleedkamerPipe } from './kleedkamer.pipe';

describe('KleedkamerPipe', () => {
  it('create an instance', () => {
    const pipe = new KleedkamerPipe();
    expect(pipe).toBeTruthy();
  });
});
