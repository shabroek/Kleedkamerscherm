import { JongbrabantPipe } from './jongbrabant.pipe';

describe('JongbrabantPipe', () => {
  it('create an instance', () => {
    const pipe = new JongbrabantPipe();
    expect(pipe).toBeTruthy();
  });
});
